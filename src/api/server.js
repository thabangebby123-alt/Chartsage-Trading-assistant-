/**\n * Express API Server for Trading Assistant\n * Provides RESTful endpoints for chart analysis, market data, and trading signals\n */\n\nimport express from 'express';\nimport dotenv from 'dotenv';\nimport chartRoutes from './routes/chartRoutes.js';\nimport analysisRoutes from './routes/analysisRoutes.js';\nimport insightsRoutes from './routes/insightsRoutes.js';\nimport entriesRoutes from './routes/entriesRoutes.js';\n\ndotenv.config();\n\nconst app = express();\nconst PORT = process.env.PORT || 3000;\nconst HOST = process.env.HOST || 'localhost';\n\n// Middleware\napp.use(express.json({ limit: '50mb' }));\napp.use(express.urlencoded({ limit: '50mb', extended: true }));\n\n// CORS Middleware\napp.use((req, res, next) => {\n  res.header('Access-Control-Allow-Origin', '*');\n  res.header('Access-Control-Allow-Headers', 'Origin, X-Requested-With, Content-Type, Accept');\n  res.header('Access-Control-Allow-Methods', 'GET, POST, PUT, DELETE, OPTIONS');\n  next();\n});\n\n// Health Check\napp.get('/api/health', (req, res) => {\n  res.json({\n    status: 'healthy',\n    timestamp: new Date().toISOString(),\n    uptime: process.uptime()\n  });\n});\n\n// API Routes\napp.use('/api/chart', chartRoutes);\napp.use('/api/analysis', analysisRoutes);\napp.use('/api/insights', insightsRoutes);\napp.use('/api/entries', entriesRoutes);\n\n// Error Handling Middleware\napp.use((err, req, res, next) => {\n  console.error('Error:', err);\n  res.status(err.status || 500).json({\n    error: err.message || 'Internal Server Error',\n    timestamp: new Date().toISOString()\n  });\n});\n\n// 404 Handler\napp.use((req, res) => {\n  res.status(404).json({\n    error: 'Not Found',\n    path: req.path,\n    method: req.method\n  });\n});\n\n// Start Server\napp.listen(PORT, HOST, () => {\n  console.log(`🚀 Trading Assistant API Server running on http://${HOST}:${PORT}`);\n  console.log(`📊 Chart Analysis: POST http://${HOST}:${PORT}/api/chart/analyze`);\n  console.log(`📈 Technical Analysis: POST http://${HOST}:${PORT}/api/analysis/technical`);\n  console.log(`💡 Market Insights: GET http://${HOST}:${PORT}/api/insights/sentiment`);\n  console.log(`🎯 Entry Detection: POST http://${HOST}:${PORT}/api/entries/detect`);\n});\n\nexport default app;\n"